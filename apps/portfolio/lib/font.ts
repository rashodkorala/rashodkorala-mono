/** Plus Jakarta Sans (+ Noto Sans Sinhala fallback) — body text, UI, navigation, labels, metadata */
export const jakartaSans = "var(--font-sans-stack)" as const;

/** Cormorant Garamond (+ Noto Serif Sinhala fallback) — display headings, hero name, pull quotes */
export const cormorantGaramond = "var(--font-serif-stack)" as const;
